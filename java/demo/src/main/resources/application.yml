spring:
  application:
    name: demo

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
      additional-paths: src/main/java

  web:
    resources:
      add-mappings: false
